<div class="container">
    <div class="row justify-content-md-center">
        <div class="col-md-12">

            <h1 class="text-center mt-4 mb-4"><%= gettext("About Us") %></h1>

            <ul class="nav nav-fill nav-pagenav m-4">
                <li class="nav-item <%= active_about_path(@conn, :index) %>">
                    <%= link gettext("About Glimesh"), to: Routes.about_path(@conn, :index), class: "nav-link" %>
                </li>
                <li class="nav-item <%= active_about_path(@conn, :streaming) %>">
                    <%= link gettext("Streaming"), to: Routes.about_path(@conn, :streaming), class: "nav-link" %>
                </li>
                <li class="nav-item <%= active_about_path(@conn, :team) %>">
                    <%= link gettext("The Team"), to: Routes.about_path(@conn, :team), class: "nav-link" %>
                </li>
                <li class="nav-item <%= active_about_path(@conn, :mission) %>">
                    <%= link gettext("Our Mission"), to: Routes.about_path(@conn, :mission), class: "nav-link" %>
                </li>
            </ul>

            <%= @inner_content %>

        </div>
    </div>
</div>
